import React from 'react';

import classes from './Stock.css';

const stock = ( props ) => (
    <div className={classes.Stock}>
        <p>Tire</p>
        <p>Brand</p>
    </div>
);

export default stock;